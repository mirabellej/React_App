import React from "react";
import { Text, StyleSheet } from "react-native";

const HomeScreen = () => {
  return <Text style={styles.text}>New Content2</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 10
  }
});

export default HomeScreen;
